@@ -1,18 +1,19 @@
# fly.toml
app = "bot-fly-b7iqvq"
primary_region = "fra"

[processes]
  app = "uvicorn main:app --host 0.0.0.0 --port 8000"

[vm]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

[build]
  # Fly сам соберёт образ из Dockerfile
  # Ничего не указываем — используем Dockerfile

[[services]]
  internal_port = 8000
  protocol = "tcp"
  process = "app"  # ← КРИТИЧНО! Привязка процесса к сервису

  [services.concurrency]
    type = "connections"
@@ -40,3 +41,6 @@ primary_region = "fra"
    path = "/"
    protocol = "http"
    timeout = "2s"
    tls_skip_verify = false

    [services.http_checks.headers]
